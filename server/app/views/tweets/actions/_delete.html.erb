<% if current_user.uid == tweet.author.id %>
<%= link_to 'Delete', url_for(action: :delete_tweet, tweet_id: tweet.id), class: "inline-block bg-red-500 text-white text-xs px-2 py-1 rounded-full mr-2" %>
<% end %>
