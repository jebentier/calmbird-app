<% if current_user.uid != tweet.author.id %>
  <%= link_to 'Quote', url_for(action: :new_quote_tweet, tweet_id: tweet.id), class: "inline-block bg-purple-500 text-white text-xs px-2 py-1 rounded-full mr-2" %>
<% end %>
