<% if current_user.uid != tweet.author.id %>
  <%= link_to 'Reply', url_for(action: :new_reply_tweet, tweet_id: tweet.id), class: "inline-block bg-teal-500 text-white text-xs px-2 py-1 rounded-full mr-2" %>
<% end %>
